export const timeZone = 'Asia/Hong_Kong';

// Change your IP address here 
// Normally, this kind of secret should put in .env file
const host = 'localhost';
export const REACT_APP_API = `http://${host}:8100`;